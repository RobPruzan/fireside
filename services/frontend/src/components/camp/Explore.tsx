import { useLoaderData } from "@tanstack/react-router";

export const Explore = () => {
  const res2 = useLoaderData({ from: "/camp/" });

  return <div className="w-full flex justify-center items-center">fdsf</div>;
};
